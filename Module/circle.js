const circleArea = (radius) => {
  console.log(Math.PI * radius ** 2);
};

const circleCircumference = (radius) => {
  console.log(Math.PI * radius * 2);
};

module.exports = {
  circleArea,
  circleCircumference,
};
